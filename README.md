# libkv
This library implements a basic in-memory key-value store. This project is meant to be used as a toy project for implementing an SDK + app level logic that allows an end user to interact with this shared code.
